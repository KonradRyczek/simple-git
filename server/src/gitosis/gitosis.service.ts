import { Injectable } from '@nestjs/common';

@Injectable()
export class GitosisService {
    

    createRepository(){


    }
    // create repo

    // delete repo

    // add ssh key

    // delete ssh key
}
